server:
  port: 8086

spring:
  application:
    name: inventory-service

  datasource:
    url: jdbc:mysql://localhost:3310/inventory-db
    username: root
    password: 141248690405Zt.
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  register-with-eureka: true
  fetch-registry: true
  instance:
    prefer-ip-address: true